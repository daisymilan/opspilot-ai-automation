# database/

Supabase PostgreSQL schema and migrations for OpsPilot's core entities
(users, organizations, leads, lead_scores, meetings, meeting_action_items,
documents, document_extractions, approvals, workflow_executions,
workflow_errors, ai_generations, notifications, audit_logs, integrations).

Schema will be introduced incrementally, entity group by entity group, once
the corresponding workflow is being built — not created wholesale up front.

Nothing here yet — populated starting the database-design phase.
