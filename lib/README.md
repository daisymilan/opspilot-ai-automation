# lib/

Framework-agnostic, domain-agnostic utilities: formatting helpers, constants,
generic type guards, shared config parsing. Code here should not know about
leads, meetings, documents, or any OpsPilot business concept — that belongs in
`services/`.

Nothing here yet — populated starting Phase 1.
