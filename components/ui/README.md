# components/ui/

Small, reusable, presentation-only UI primitives (buttons, cards, badges,
inputs, tables) with no business logic and no data fetching. Feature-specific
components live alongside the routes that use them or in `components/` directly.
