import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Organization, integration, and notification settings."
      phase="Populated alongside authentication and integrations"
    />
  );
}
