import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function MeetingsPage() {
  return (
    <PagePlaceholder
      title="Meetings"
      description="Meeting and voice-note transcripts with AI-generated summaries, extracted action items, owners, and due dates."
      phase="Populated in the AI Meeting Intelligence phase"
    />
  );
}
