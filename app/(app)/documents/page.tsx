import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function DocumentsPage() {
  return (
    <PagePlaceholder
      title="Documents"
      description="Uploaded documents with extracted text, AI analysis, and schema-validated structured output."
      phase="Populated in the AI Document Intelligence phase"
    />
  );
}
