import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function LeadsPage() {
  return (
    <PagePlaceholder
      title="Leads"
      description="Leads captured via webhook, API, or manual form, with AI classification, scoring, and recommended next action."
      phase="Populated in the AI Lead Intelligence phase"
    />
  );
}
